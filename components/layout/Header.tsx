import styles from "@/styles/Layout.module.css"

function Header() {
  return (
    <header className={styles.mainHeader} id="header">
      <h1>Next Basic test app for practice</h1>
    </header>
  );
}

export default Header;
